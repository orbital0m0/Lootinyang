# 🐱 습관 형성 고양이 앱 - 설계 문서

## 📋 프로젝트 개요
토스 미니앱 기반의 습관 형성 앱으로, 사용자가 주 N회 습관을 설정하고 매일 체크하며 진행률을 확인하고 보상을 받는 게이미피케이션 요소를 포함합니다.

## 🎯 핵심 기능
1. **습관 생성**: 주 N회 선택 및 습관 설정
2. **일일 체크**: 진행 바 시각화
3. **보상 시스템**: 랜덤 아이템 상자, 보호 아이템
4. **업적 시스템**: 3주 연속 성공, 누적 업적, 특별 상자/뱃지
5. **캐릭터**: 고양이 메인 캐릭터 및 관련 아이템

## 🏗️ 아키텍처 설계

### 기술 스택
- **Frontend**: React + TypeScript + Vite
- **상태 관리**: TanStack Query (서버 상태) + Context (클라이언트 상태)
- **스타일링**: Tailwind CSS + Framer Motion
- **백엔드**: Supabase (데이터베이스 + 인증)
- **라우팅**: React Router DOM
- **플랫폼**: 토스 미니앱 SDK

### 데이터 모델
```typescript
// 사용자
interface User {
  id: string;
  username: string;
  level: number;
  exp: number;
  totalHabits: number;
  streak: number;
}

// 습관
interface Habit {
  id: string;
  userId: string;
  name: string;
  weeklyTarget: number; // 주 N회
  createdAt: Date;
  isActive: boolean;
}

// 일일 체크
interface DailyCheck {
  id: string;
  habitId: string;
  date: Date;
  completed: boolean;
}

// 아이템
interface Item {
  id: string;
  name: string;
  type: 'random' | 'protection' | 'special';
  rarity: 'common' | 'rare' | 'epic' | 'legendary';
  description: string;
  icon: string;
}

// 보상 상자
interface RewardBox {
  id: string;
  userId: string;
  type: 'daily' | 'weekly' | 'monthly' | 'special';
  isOpened: boolean;
  items: Item[];
  createdAt: Date;
}

// 업적
interface Achievement {
  id: string;
  name: string;
  description: string;
  icon: string;
  condition: string;
  points: number;
}
```

### 컴포넌트 구조
```
src/
├── components/
│   ├── common/
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── ProgressBar.tsx
│   │   └── Modal.tsx
│   ├── habit/
│   │   ├── HabitForm.tsx
│   │   ├── HabitList.tsx
│   │   └── HabitCard.tsx
│   ├── reward/
│   │   ├── RewardBox.tsx
│   │   ├── ItemDisplay.tsx
│   │   └── AchievementBadge.tsx
│   └── character/
│       ├── CatCharacter.tsx
│       └── CatAnimation.tsx
├── pages/
│   ├── Home.tsx
│   ├── Habits.tsx
│   ├── Rewards.tsx
│   ├── Profile.tsx
│   └── Achievements.tsx
├── hooks/
│   ├── useHabits.ts
│   ├── useRewards.ts
│   └── useAchievements.ts
├── services/
│   ├── supabase.ts
│   ├── habitService.ts
│   └── rewardService.ts
└── utils/
    ├── constants.ts
    ├── helpers.ts
    └── animations.ts
```

## 🎮 게이미피케이션 설계

### 진행 시스템
- **레벨**: 경험치 기반 (1-100)
- **스트릭**: 연속 체크 일수
- **포인트**: 습관 완료 시 획득

### 보상 시스템
- **일일 상자**: 하루 목표 달성 시
- **주간 상자**: 주 목표 달성 시
- **특별 상자**: 3주 연속 성공 시
- **아이템 종류**:
  - 랜덤 아이템 (고양이 장난감, 간식 등)
  - 보호 아이템 (하루 계획 보호)
  - 특별 아이템 (경험치 증가, 레벨업 보너스)

### 업적 시스템
- **초보**: 첫 습관 생성
- **꾸준함**: 7일 연속 체크
- **달성자**: 주 목표 3회 달성
- **마스터**: 30일 연속 체크
- **전설**: 100일 연속 체크

## 🎨 디자인 시스템

### 컬러 팔레트
- **Primary**: 핑크 계열 (#f43f5e)
- **Secondary**: 고양이 오렌지 (#ff8c42)
- **Accent**: 보라색 계열 (#b19cd9)
- **Neutral**: 회색 계열

### 타이포그래피
- **제목**: Inter Bold (24-32px)
- **본문**: Inter Regular (14-16px)
- **라벨**: Inter Medium (12-14px)

### 애니메이션
- **고양이**: 깡충깡충, 꼬리 흔들기
- **진행바**: 부드러운 채우기
- **상자 오픈**: 입자 효과, 반짝임
- **업적**: 팝업, 배지 획득 애니메이션

## 📱 토스 미니앱 연동

### SDK 기능
- **사용자 인증**: 토스 계정 연동
- **결제**: 유료 아이템 구매 (선택적)
- **공유**: 업적 공유 기능
- **알림**: 습관 리마인더

### 레이아웃 제약
- **최대 너비**: 375px (모바일 기준)
- **세로 스크롤**: 제한 없음
- **하단 네비게이션**: 토스 가이드라인 따름

## 🔧 개발 우선순위

### Phase 1: 기본 기능
1. 프로젝트 설정 및 기본 구조
2. Supabase 연동 및 데이터 모델
3. 습관 생성/수정/삭제 기능
4. 일일 체크 기능

### Phase 2: 게이미피케이션
1. 진행 바 및 레벨 시스템
2. 보상 상자 기능
3. 아이템 획득 및 관리
4. 고양이 캐릭터 애니메이션

### Phase 3: 고급 기능
1. 업적 시스템
2. 통계 및 분석
3. 소셜 기능 (공유)
4. 토스 SDK 연동

## 🚀 배포 및 운영
- **개발 환경**: 로컬 Vite 서버
- **테스트**: Supabase 스테이징 환경
- **프로덕션**: 토스 미니앱 스토어 배포
- **모니터링**: Supabase 대시보드 + 사용자 행동 추적