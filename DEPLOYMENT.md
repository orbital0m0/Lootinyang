# 🚀 배포 가이드

## 준비 사항

### 1. Supabase 프로젝트 설정
```bash
# 1. Supabase Dashboard 접속 (https://app.supabase.com)
# 2. 새 프로젝트 생성
# 3. Database SQL Editor에서 테이블 생성
# 4. API Keys 확인 (Project URL, anon public key)
```

### 2. 환경 변수 설정
```bash
# .env 파일 생성
cp .env.example .env

# .env 파일에 Supabase 정보 입력
# VITE_SUPABASE_URL=your-supabase-project-url
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 3. 의존성 설치
```bash
npm install
```

## 빌드

### 1. 개발 빌드
```bash
npm run dev
```

### 2. 프로덕션 빌드
```bash
npm run build
```

### 3. 빌드 결과 확인
- `dist/` 폴더가 생성됩니다
- `index.html` 및 정적 에셋이 생성됩니다
- 번들 크기: ~650KB (gzip: ~200KB)

## 테스트

### 1. 단위 테스트 실행
```bash
npm test
```

### 2. 테스트 커버리지 실행
```bash
npm run test:coverage
```

### 3. 워치 모드 테스트
```bash
npm run test:watch
```

## 배포

### 옵션 1: Vercel (권장)
```bash
# Vercel CLI 설치
npm install -g vercel

# 배포
vercel --prod
```

### 옵션 2: Netlify
```bash
# Netlify CLI 설치
npm install -g netlify-cli

# 배포
netlify deploy --prod --dir=dist
```

### 옵션 3: GitHub Pages
```bash
# 빌드 후 dist 폴더를 gh-pages 브랜치에 푸시
npm run build
git subtree push --prefix dist origin gh-pages
```

## 배포 후 점검 사항

### 1. 환경 변수 확인
- Supabase URL과 키가 정확히 설정되었는지 확인

### 2. 빌드 설정
- 빌드 경로가 정확한지 확인
- 번들 크기가 최적화되었는지 확인 (현재 ~200KB gzip)

### 3. 라우팅 설정
- 배포 후 라우팅이 정상 작동하는지 확인
- 토스 미니앱 하단 네비게이션 호환성 확인

### 4. API 연동
- Supabase와의 통신이 정상 작동하는지 확인
- 쿼리 정책 및 CORS 설정 확인

## 토스 미니앱 특별 사항

### 1. 하단 네비게이션
- 토스 미니앱은 하단 네비게이션을 요구합니다
- `react-router-dom`의 `<Link>` 컴포넌트를 사용하여 구현했습니다

### 2. 최대 너비
- 토스 미니앱의 최대 너비는 375px입니다
- CSS에서 `max-w-md` 클래스를 사용하여 제한했습니다

### 3. 세이프 에어리어
- 토스 미니앱의 세이프 에어리어를 사용하지 마세요
- 모든 스타일을 inline으로 포함해야 합니다

## 트러블슈팅

### 1. package.json JSON 파싱 오류

**증상:**
```bash
npm error code EJSONPARSE
npm error JSON.parse Unexpected token "v" (0x76)
```

**원인:**
- `package.json`의 `scripts` 섹션에서 JSON 문법 오류
- 닫는 따옴표(`"`) 누락 또는 이중 따옴표 사용 오류

**해결 방법:**
1. `package.json` 파일을 열어 `scripts` 섹션 확인
2. 다음 사항을 점검:
   - 모든 문자열은 쌍따옴표(`"`)로 감싸야 함
   - 키와 값 사이에는 콜론(`:`)이 필요
   - 각 항목은 콤마(`,`)로 구분
   - 마지막 항목 뒤에는 콤마 없음
3. 빌드 테스트로 검증: `npm run build`

**예시 (잘못된 예시):**
```json
"scripts": {
  "deploy: "vercel --prod",
  ...
}
```

**예시 (올바른 예시):**
```json
"scripts": {
  "deploy": "vercel --prod",
  ...
}
```

### 2. 빌드 실패

**증상:**
- `npm run build` 실행 중 오류 발생
- TypeScript 타입 오류 또는 모듈 찾기 실패

**해결 방법:**
1. TypeScript 타입 체크: `npm run type-check`
2. Lint 실행: `npm run lint`
3. Lint 자동 수정: `npm run lint:fix`
4. 의존성 재설치:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

### 3. 배포 후 라우팅 오류

**증상:**
- 배포 후 페이지 새로고침 시 404 오류
- SPA 라우팅이 작동하지 않음

**해결 방법:**
1. Vercel 설정 확인: `vercel.json`
2. Netlify 설정: `_redirects` 파일 생성
3. GitHub Pages 설정: `vite.config.ts`의 `base` 경로 설정

**Vercel 설정 예시:**
```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```
